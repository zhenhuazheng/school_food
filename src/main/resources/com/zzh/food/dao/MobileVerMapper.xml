<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzh.food.dao.FoodMobileVerMapper">

    <!-- 根据用户名查询用户信息 -->
    <select id="findMobileVerByPhone" resultType="com.zzh.food.entity.MobileVerEntity">
        select * from food_mobile_ver where phone=#{phone} order by createTime desc limit 1;
    </select>


    <!-- 后台添加用户，返回添加的用户Id -->
    <insert id="addMobileVer" parameterType="com.zzh.food.vo.MobileVerVo">
        <!-- 获取insert的用户ID -->
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
            select last_insert_id()
        </selectKey>
        insert into food_mobile_ver (verCode, phone, endTime, createTime, status)
        values(#{verCode}, #{phone}, #{endTime}, NOW(), 'N');
    </insert>

    <!-- 后台修改用户信息 -->
    <update id="modifyMobileVer" parameterType="com.zzh.food.vo.MobileVerVo">
        update food_mobile_ver
        <set>
            <if test="status!=null and status!=''"> status=#{status}, </if>
            <if test="endTime!=null"> endTime=#{endTime}, </if>
        </set>
        where id=#{id}
    </update>

</mapper>